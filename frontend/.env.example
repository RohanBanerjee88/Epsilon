# Development defaults. Production uses the current site origin for both.
NEXT_PUBLIC_API_URL=http://localhost:8080

# Optional: set both URLs to your computer's LAN address when testing QR cards
# on a physical phone, for example http://192.168.1.20:3000.
NEXT_PUBLIC_SHARE_BASE_URL=
